use gpui::*;

impl super::super::ControlCenterWidget {
    pub(in crate::widgets::control_center) fn render_proxy_details(
        &mut self,
    ) -> AnyElement {
        // TODO: Implémenter proxy details
        div().into_any_element()
    }
}
